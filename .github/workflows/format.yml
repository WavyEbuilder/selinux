name: Check format

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  check-format:
    runs-on: fedora-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check format
        run: make check-format
